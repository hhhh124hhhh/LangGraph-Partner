# AI Partner Demo 后端依赖 (简化版)
# 基于现有LangGraph环境，只添加Demo特有的依赖

# FastAPI 核心 (如果现有环境没有)
fastapi>=0.104.1
uvicorn[standard]>=0.24.0

# WebSockets 支持 (如果现有环境没有)
websockets>=12.0

# 数据验证和序列化
pydantic>=2.5.0
pydantic-settings>=2.1.0

# HTTP客户端 (如果现有环境没有)
httpx>=0.25.2
requests>=2.31.0

# CORS 支持 (如果现有环境没有)
python-multipart>=0.0.6

# 开发和测试工具
pytest>=7.4.3
pytest-asyncio>=0.21.1

# 日志和监控 (可选)
structlog>=23.2.0